
// const SafeHtmlContent = ({ htmlContent }) => {
//   // Sanitize the HTML to prevent XSS attacks
//   const sanitizedHTML = DOMPur.sanitize(htmlContent);

//   return (
//     <div
//       dangerouslySetInnerHTML={{ __html: sanitizedHTML }}
//     />
//   );
// };

// export default SafeHtmlContent;